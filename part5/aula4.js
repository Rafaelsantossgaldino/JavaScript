// Arrow  function normal e simplificada

// Normal
const doubleVelocity = (velocity) => {
  return velocity * 2
}

// Simplificada
const doubleSpeed = (velocity) => velocity * 2

console.log(doubleVelocity(60))
console.log(doubleSpeed(10))